#include <stdio.h>
int main()
{
    float Math = 85;      // marks
    float Physics = 53;   // marks
    float Chemistry = 69; // marks
    float English = 73;   // marka
    float Hindi = 82;     // marks
    float Precentage = (Math + Physics + Chemistry + English + Hindi) / 5;
    printf("Percentage of five subject =%f", Precentage);
    return 0;
}